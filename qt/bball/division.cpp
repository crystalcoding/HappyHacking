#include "division.h"

Division::Division()
{
}

std::vector<Round> Division::generate_rounds()
{
    //a list of team
    //round robin -> a vector of matches -> form a round
    //repeat
    //a vector of rounds
    Round r;
    std::vector<Round> rv;
    rv.push_back(r);
    return rv;
}
