#include <vector>
#include <string>

using namespace std;

int main()
{
  typedef unsigned char UC;
  typedef const unsigned char CNC;
  typedef int* PI; //!!!
  typedef char** PPC; //!!!
  typedef vector<char*>* PVC;
  typedef vector<int (*)[7] > V7P;
  typedef vector<int (*)[7]>* PV7P;
  //typedef vector<vector<int*[7]> > V87P;
  typedef vector<V7P[8]> V87P; 

  
}
